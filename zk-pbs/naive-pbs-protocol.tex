\begin{table}[h!]
\centering

% Rename "Table" to "Protocol" for this float
\renewcommand{\tablename}{Protocol}

\begin{tblr}{
    hline{1,Z} = {solid, 1pt},
    colspec     = { Q[c,$] c Q[c,$] },
    row{1-2}    = {mode=text},
    row{3-Z}    = {rowsep=3ex}
}

% Participating Entities
Proposer & & Builder_{j}\\

% Step 1 - pre processing
$\begin{gathered}[b]
    B_{i, j-1}, F_{i, j-1}
\end{gathered}$ \\

% Step 2 - Block hash submission
\begin{gathered}[b]
    B_{i, j} := B_{i, j-1} \cup h(b_{i,j}) \\
    F_{i, j} := f_{i,j-1} \cup f_{i,j}
\end{gathered} &
\tikz \draw[<-] (0,0) to node[above] {$h(b_{i,j}) || f_{i,j}$} (2.3,0);
&
$b_{i,j}, f_{i,j}$ \\

% Step 3 - Block hash selection and signing
\begin{gathered}[b]
s = \arg\max_{b_{i,k} \in B_{i,j}} f_k \\
m=h(b_{i,s}) || f_{i,s} || s
\end{gathered}
&
\tikz \draw[->] (0,0) to node[above] {$m || sig(m)$} (2.3,0);
\\

% Step 4 - Block submission
\begin{gathered}[b]
Gossip \ b_{i,s}
\end{gathered}
& 
\tikz \draw[<-] (0,0) to ["$b_{i,s}$"] (2.3,0);
\\

% Step 5 - Rewards
$f_{i,s} := r_p + r_b$
& 
\tikz \draw[->] (0,0) to ["$r_b$"] (2.3,0);
\\

\end{tblr}

\caption{Naive Proposer-Builder Separation}
\label{protocol:naive_proposer_builder_separation}

\end{table}
